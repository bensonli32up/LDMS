package ldms.technical.challenge.config;

import org.hibernate.dialect.Dialect;

/**
 * A custom Hibernate dialect for SQLite.
 */
@SuppressWarnings("ALL")
public class SQLiteDialect extends Dialect {
}